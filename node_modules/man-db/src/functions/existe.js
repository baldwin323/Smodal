const fs = require( 'fs' )
const path = require( 'path' )
const choco = {}

module.exports = {
    existe: function ( directorio ) {
        if ( fs.existsSync( `./choco_folder/${ directorio }.json` ) ) {
            console.log( "El JSON existe ✔" );
        } else {
            console.log( "El JSON no existe ❌" );
        }
    }
}