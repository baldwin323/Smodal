const choco = {};
const fs = require( 'fs' )
module.exports = {
    nuevoJSON: function ( nombre, ) {
        if ( !nombre ) return console.log( 'Ingresa un nombre' )
        fs.mkdir( 'choco_folder', function ( err ) {
            if ( err ) {
            } else {
            }
        } )
        fs.writeFile( `./choco_folder/${ nombre }.json`, `{}`, function ( err ) {
            if ( err ) {
                console.log( 'No se puedo crear el archivo .json' );
            } else {
                console.log( `Se hizo ${ nombre }.json, es recomendable que no lo toques ni lo edites, asi no se corrompe nada` )
            }
        } )
    }
}